{% extends 'base.html.twig' %}

{% block body %}
    <h1>PeriodeReservation</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ periodeReservation.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ periodeReservation.libelle }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ periodeReservation.amount }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ periodeReservation.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('periodereservation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('periodereservation_edit', { 'id': periodeReservation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
